creaet this sample excel file:

Month	Marketing_Spend	Sales
Jan	2000	22000
Feb	2500	25000
Mar	3000	27000
Apr	3500	30000
May	4000	32000
Jun	4500	35000
Jul	5000	37000
Aug	5500	40000
Sep	6000	42000
Oct	6500	45000
Nov	7000	48000
Dec	7500	50000


# 'dataset' holds the input data for this script
import pandas as pd
from sklearn.linear_model import LinearRegression

# Load dataset from Power BI
df = dataset

# Ensure numeric
df['Marketing_Spend'] = pd.to_numeric(df['Marketing_Spend'])
df['Sales'] = pd.to_numeric(df['Sales'])

# Model
X = df[['Marketing_Spend']]
y = df['Sales']

model = LinearRegression()
model.fit(X, y)

# Add predictions to dataset
df['Predicted_Sales'] = model.predict(X)

# Return dataset
df

# Return dataset
intercept = model.intercept_
coefficient = model.coef_[0]
df['Intercept'] = intercept
df['Coefficient'] = coefficient
df
=================================================

import matplotlib.pyplot as plt

df = dataset

# Actual points
plt.scatter(df['Marketing_Spend'], df['Sales'], label='Actual')

# Predicted points
plt.scatter(df['Marketing_Spend'], df['Predicted_Sales'], label='Predicted')

# Regression line
plt.plot(df['Marketing_Spend'], df['Predicted_Sales'], label='Regression Line')

